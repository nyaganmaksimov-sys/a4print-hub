self.addEventListener('push',event=>{let data={};try{data=event.data?event.data.json():{}}catch{data={body:event.data?.text()||'Новая доставка ЦУИМ'}};const title=data.title||'ЦУИМ Доставка';const options={body:data.body||'Появилась новая доставка',tag:data.tag||'cuim-delivery',renotify:true,data:{url:data.url||'./?tab=exchange'},icon:'../admin/assets/logo_bd_transparent.svg',badge:'../admin/assets/logo_bd_transparent.svg'};event.waitUntil(self.registration.showNotification(title,options))});self.addEventListener('notificationclick',event=>{event.notification.close();const target=new URL(event.notification.data?.url||'./?tab=exchange',self.location.origin+'/courier/').href;event.waitUntil(clients.matchAll({type:'window',includeUncontrolled:true}).then(list=>{for(const c of list){if(c.url.startsWith(self.location.origin+'/courier/')){c.focus();c.navigate(target);return}}return clients.openWindow(target)}))});